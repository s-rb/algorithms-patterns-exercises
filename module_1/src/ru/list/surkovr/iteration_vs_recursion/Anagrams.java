package ru.list.surkovr.iteration_vs_recursion;

// todo
public class Anagrams {

    /*public static List<String> getRecursive(String src) {
        List<String> res = new LinkedList<>();
        int length = src.length();
        if (length == 1) {
            res.add(src);
            return res;
        }
        char firstChar = src.charAt(0);
        for (int i = 1; i < src.length(); i++) {

        }

        res.addAll(getRecursive())
    }

    public static List<String> getIterative(String src) {
        return "";
    }*/
}
